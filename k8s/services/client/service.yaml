apiVersion: v1
kind: Service
metadata:
  name: client-service-{{ENVIRONMENT}}
  namespace: "{{ENVIRONMENT}}"
spec:
  ports:
    - name: "80"
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: client
  type: LoadBalancer
